--[[
    Script: Atlas Hub - Bee Swarm Simulator 1:1 (Bản sửa lỗi)
    Sử dụng Rayfield Library
    Chạy ổn định hơn với vòng lặp trung tâm.
]]

-- Tải Rayfield
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Cửa sổ chính
local Window = Rayfield:CreateWindow({
    Name = "Atlas Hub - Bee Swarm 1:1",
    LoadingTitle = "Đang tải...",
    LoadingSubtitle = "Bởi AI Assistant",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "AtlasConfig",
        FileName = "AtlasHub"
    },
})

-- Biến toàn cục
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local VirtualUser = game:GetService("VirtualUser")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local Settings = {
    SelectedField = "Dandelion Field",
    AutoFarm = false,
    AutoCollect = false,
    AutoTokenLink = false,
    AutoConvert = false,
    AutoMapAbilities = false,
    AutoKillMonsters = false,
    AutoDispenser = false,
    AutoFieldBoosters = false,
    TweenSpeed = 16,
}

-- ==================== TAB ====================
local MainTab = Window:CreateTab("Main", 4483362458)
local CombatTab = Window:CreateTab("Bees & Combat", 4483362458)
local DispenserTab = Window:CreateTab("Dispensers", 4483362458)
local SettingsTab = Window:CreateTab("Settings", 4483362458)

-- Dropdown Field
MainTab:CreateDropdown({
    Name = "Chọn Cánh Đồng",
    Options = {"Dandelion Field", "Sunflower Field", "Mushroom Field", "Blue Flower Field", "Clover Field", "Strawberry Field", "Bamboo Field", "Pine Tree Forest", "Pumpkin Patch", "Mountain Top Field", "Cactus Field", "Rose Field", "Spider Field", "Pepper Patch", "Coconut Field", "Stump Field"},
    CurrentOption = "Dandelion Field",
    Flag = "FieldDropdown",
    Callback = function(Option)
        Settings.SelectedField = Option
        print("Đã chọn field:", Option)
    end,
})

-- Toggle Auto Farm
MainTab:CreateToggle({
    Name = "Auto Farm Field",
    CurrentValue = false,
    Flag = "AutoFarm",
    Callback = function(Value)
        Settings.AutoFarm = Value
        print("Auto Farm:", Value)
    end,
})

-- Toggle Auto Collect
MainTab:CreateToggle({
    Name = "Auto Collect Vật Phẩm",
    CurrentValue = false,
    Flag = "AutoCollect",
    Callback = function(Value)
        Settings.AutoCollect = Value
        print("Auto Collect:", Value)
    end,
})

-- Toggle Auto Token Link
MainTab:CreateToggle({
    Name = "Auto Token Link",
    CurrentValue = false,
    Flag = "AutoToken",
    Callback = function(Value)
        Settings.AutoTokenLink = Value
        print("Auto Token Link:", Value)
    end,
})

-- Toggle Auto Convert
MainTab:CreateToggle({
    Name = "Auto Convert Pollen Khi Đầy",
    CurrentValue = false,
    Flag = "AutoConvert",
    Callback = function(Value)
        Settings.AutoConvert = Value
        print("Auto Convert:", Value)
    end,
})

-- Combat
CombatTab:CreateToggle({
    Name = "Auto Map Abilities",
    CurrentValue = false,
    Flag = "AutoMapAbilities",
    Callback = function(Value)
        Settings.AutoMapAbilities = Value
        print("Auto Map Abilities:", Value)
    end,
})

CombatTab:CreateToggle({
    Name = "Auto Kill Monsters",
    CurrentValue = false,
    Flag = "AutoKill",
    Callback = function(Value)
        Settings.AutoKillMonsters = Value
        print("Auto Kill Monsters:", Value)
    end,
})

-- Dispensers
DispenserTab:CreateToggle({
    Name = "Auto Free Dispenser",
    CurrentValue = false,
    Flag = "AutoDispenser",
    Callback = function(Value)
        Settings.AutoDispenser = Value
        print("Auto Dispenser:", Value)
    end,
})

DispenserTab:CreateToggle({
    Name = "Auto Field Boosters",
    CurrentValue = false,
    Flag = "AutoBooster",
    Callback = function(Value)
        Settings.AutoFieldBoosters = Value
        print("Auto Field Boosters:", Value)
    end,
})

-- Settings
SettingsTab:CreateSlider({
    Name = "Tốc độ di chuyển",
    Range = {5, 60},
    Increment = 1,
    Suffix = "studs/s",
    CurrentValue = 16,
    Flag = "TweenSpeed",
    Callback = function(Value)
        Settings.TweenSpeed = Value
        print("Tween Speed:", Value)
    end,
})

-- ==================== HÀM HỖ TRỢ ====================
local function getCharacter()
    return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
end

local function getHumanoid()
    return getCharacter():WaitForChild("Humanoid")
end

local function getRootPart()
    return getCharacter():WaitForChild("HumanoidRootPart")
end

-- Tìm object theo tên
local function findObject(name, parent)
    parent = parent or Workspace
    for _, obj in ipairs(parent:GetDescendants()) do
        if obj.Name == name then
            return obj
        end
    end
    return nil
end

-- Lấy vị trí cánh đồng
local function getFieldPosition(fieldName)
    -- CẦN CHỈNH: Tìm đúng object trong game
    local fieldObj = findObject(fieldName)
    if fieldObj then
        if fieldObj:IsA("BasePart") then
            return fieldObj.Position
        elseif fieldObj:IsA("Model") then
            local primary = fieldObj.PrimaryPart
            if primary then return primary.Position end
        end
    end
    warn("Không tìm thấy field:", fieldName)
    return nil
end

-- Lấy vị trí Hive
local function getHivePosition()
    -- CẦN CHỈNH: Tìm Hive
    local hive = findObject("Hive")
    if hive then
        if hive:IsA("BasePart") then
            return hive.Position
        elseif hive:IsA("Model") then
            local primary = hive.PrimaryPart
            if primary then return primary.Position end
        end
    end
    warn("Không tìm thấy Hive")
    return nil
end

-- Kiểm tra phấn hoa đầy
local function isPollenFull()
    -- CẦN CHỈNH: Kiểm tra giá trị phấn hoa
    local pollen = LocalPlayer:FindFirstChild("Pollen") or LocalPlayer:FindFirstChild("PollenCount")
    local maxPollen = LocalPlayer:FindFirstChild("MaxPollen") or LocalPlayer:FindFirstChild("PollenCapacity")
    if pollen and maxPollen then
        return pollen.Value >= maxPollen.Value
    end
    return false
end

-- Hàm di chuyển an toàn
local function moveToPosition(position)
    if not position then return end
    local humanoid = getHumanoid()
    local root = getRootPart()
    humanoid:MoveTo(position)
    -- Chờ tối đa 10 giây để đến nơi
    local timeout = 0
    repeat
        task.wait()
        timeout = timeout + task.wait()
    until (root.Position - position).Magnitude < 5 or timeout > 10
end

-- ==================== CÁC HÀNH ĐỘNG ====================
local function actionAutoFarm()
    if not Settings.AutoFarm then return end
    local fieldName = Settings.SelectedField
    if fieldName then
        local pos = getFieldPosition(fieldName)
        if pos then
            moveToPosition(pos)
            -- CẦN CHỈNH: Thêm hành động thu hoạch
            local tool = getCharacter():FindFirstChildOfClass("Tool")
            if tool then tool:Activate() end
        end
    end
end

local function actionAutoCollect()
    if not Settings.AutoCollect then return end
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("BasePart") and obj:GetAttribute("Collectible") == true then
            local root = getRootPart()
            if (obj.Position - root.Position).Magnitude < 10 then
                moveToPosition(obj.Position)
                task.wait(0.2)
            end
        elseif obj:IsA("Tool") and (obj.Name:lower():find("token") or obj.Name:lower():find("ability")) then
            local handle = obj:FindFirstChild("Handle")
            if handle then
                local root = getRootPart()
                if (handle.Position - root.Position).Magnitude < 10 then
                    moveToPosition(handle.Position)
                    task.wait(0.2)
                end
            end
        end
    end
end

local function actionAutoTokenLink()
    if not Settings.AutoTokenLink then return end
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("BasePart") and obj.Name:lower():find("token") and obj.Name:lower():find("link") then
            local root = getRootPart()
            if (root.Position - obj.Position).Magnitude < 20 then
                moveToPosition(obj.Position)
                task.wait(0.5)
                getHumanoid():ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
    end
end

local function actionAutoConvert()
    if not Settings.AutoConvert then return end
    if isPollenFull() then
        local hivePos = getHivePosition()
        if hivePos then
            moveToPosition(hivePos)
            task.wait(1)
            getHumanoid():ChangeState(Enum.HumanoidStateType.Jumping)
            task.wait(2)
            if Settings.SelectedField then
                local fieldPos = getFieldPosition(Settings.SelectedField)
                if fieldPos then moveToPosition(fieldPos) end
            end
        end
    end
end

local function actionAutoMapAbilities()
    if not Settings.AutoMapAbilities then return end
    for _, obj in ipairs(Workspace:GetDescendants()) do
        if obj:IsA("Tool") and obj.Name:lower():find("ability") then
            local handle = obj:FindFirstChild("Handle")
            if handle then
                local root = getRootPart()
                if (root.Position - handle.Position).Magnitude < 10 then
                    moveToPosition(handle.Position)
                    task.wait(0.3)
                end
            end
        end
    end
end

local function actionAutoKillMonsters()
    if not Settings.AutoKillMonsters then return end
    local monsterNames = {"Ladybug", "Rhino Beetle", "Spider", "Werewolf", "Coconut Crab"}
    for _, name in ipairs(monsterNames) do
        local monster = findObject(name)
        if monster then
            local monsterPart = monster:IsA("Model") and monster.PrimaryPart or monster
            if monsterPart then
                moveToPosition(monsterPart.Position)
                task.wait(0.5)
                local weapon = getCharacter():FindFirstChildOfClass("Tool")
                if weapon then weapon:Activate() end
                VirtualUser:Button1Down()
                task.wait(0.5)
                VirtualUser:Button1Up()
                task.wait(1)
            end
        end
    end
end

local function actionAutoDispenser()
    if not Settings.AutoDispenser then return end
    local dispenserNames = {"Blue Dispenser", "Red Dispenser", "Treat Dispenser", "Glue Dispenser", "Coconut Dispenser"}
    for _, dispName in ipairs(dispenserNames) do
        local disp = findObject(dispName)
        if disp then
            local dispPart = disp:IsA("Model") and disp.PrimaryPart or disp
            if dispPart then
                moveToPosition(dispPart.Position)
                task.wait(0.5)
                getHumanoid():ChangeState(Enum.HumanoidStateType.Jumping)
                task.wait(0.5)
            end
        end
    end
end

local function actionAutoFieldBoosters()
    if not Settings.AutoFieldBoosters then return end
    local boosterNames = {"Mountain Top Booster", "Blue HQ Booster", "Red HQ Booster"}
    for _, bName in ipairs(boosterNames) do
        local booster = findObject(bName)
        if booster then
            local boosterPart = booster:IsA("Model") and booster.PrimaryPart or booster
            if boosterPart then
                moveToPosition(boosterPart.Position)
                task.wait(0.5)
                getHumanoid():ChangeState(Enum.HumanoidStateType.Jumping)
                task.wait(0.5)
            end
        end
    end
end

-- ==================== VÒNG LẶP TRUNG TÂM ====================
local function centralLoop()
    while true do
        -- Thực hiện các hành động, mỗi hành động có kiểm tra cờ
        actionAutoFarm()
        actionAutoCollect()
        actionAutoTokenLink()
        actionAutoConvert()
        actionAutoMapAbilities()
        actionAutoKillMonsters()
        actionAutoDispenser()
        actionAutoFieldBoosters()
        task.wait() -- quan trọng để không crash
    end
end

-- ==================== KHỞI ĐỘNG ====================
task.spawn(centralLoop)

-- Auto Reconnect
task.spawn(function()
    while true do
        if not LocalPlayer:IsConnected() then
            warn("Mất kết nối! Thử lại...")
            game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
            task.wait(10)
        end
        task.wait(5)
    end
end)

-- Thông báo
Rayfield:Notify({
    Title = "Atlas Hub",
    Content = "Script đã chạy! Hãy bật các tính năng từ menu.",
    Duration = 5,
    Image = 4483362458,
})
